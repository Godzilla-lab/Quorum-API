# Secrets. Never commit these.
.env
.env.*

# The corpus is data, not source. It is also the moat.
*.db
*.db-shm
*.db-wal
corpus/
!packages/corpus/

# Generated reports
out/
reports/

# Build output
dist/
*.tsbuildinfo

# Fixtures captured live that have not been reviewed for personal data
fixtures/raw/

node_modules/
.DS_Store
coverage/

# Internal working documents. Kept on disk, never published: the working
# agreement, the spec and PRD with their pricing and risk sections, the task
# log, and the agent skills. None of it is a secret and all of it is thinking
# rather than product, which is a different reason not to publish it.
CLAUDE.md
SPEC.md
docs/PRD.md
tasks/
.claude/

# npm publish staging, built by scripts/publish-npm.mjs
.npm-staging/
